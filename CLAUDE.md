# Scene Agent Skills contributor context

Read AGENTS.md for repository conventions. The same canonical `skills/` tree supports Claude Code and Codex. A Claude plugin does not automatically load this file into every skill invocation; essential instructions are bundled with each skill.

Use actual tools and native verification. Never present a generated recipe/script as a completed DCC operation. Keep IFC export separate from unverified native Archicad import. Run the appropriate test suites and preserve client confidentiality.
